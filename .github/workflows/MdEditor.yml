name: MdEditor

on:
  pull_request:
    branches: [ "develop" ]

jobs: 
  build:
    name: Build app
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build
        run: ./.github/build.sh